// Auto-generated Telegram Bot API models for C++
// Generated by telegram-api-generator. Do not edit manually.

#include "types/LinkPreviewOptions.hpp"
#include <nlohmann/json.hpp>

namespace TgBot {
    void to_json(json& j, const LinkPreviewOptions& value) {
        j = json{};
    if (value.is_disabled) { 
                    j["is_disabled"] = value.is_disabled; 
            }
    if (!value.url.empty()) { 
                    j["url"] = value.url; 
            }
    if (value.prefer_small_media) { 
                    j["prefer_small_media"] = value.prefer_small_media; 
            }
    if (value.prefer_large_media) { 
                    j["prefer_large_media"] = value.prefer_large_media; 
            }
    if (value.show_above_text) { 
                    j["show_above_text"] = value.show_above_text; 
            }
    }

    void from_json(const json& j, LinkPreviewOptions& value) {
        if (j.contains("is_disabled")) {
            j.at("is_disabled").get_to(value.is_disabled);
        }
        if (j.contains("url")) {
            j.at("url").get_to(value.url);
        }
        if (j.contains("prefer_small_media")) {
            j.at("prefer_small_media").get_to(value.prefer_small_media);
        }
        if (j.contains("prefer_large_media")) {
            j.at("prefer_large_media").get_to(value.prefer_large_media);
        }
        if (j.contains("show_above_text")) {
            j.at("show_above_text").get_to(value.show_above_text);
        }
    }
}
