// Auto-generated Telegram Bot API models for C++
// Generated by telegram-api-generator. Do not edit manually.

#include "requests/UploadStickerFileRequest.hpp"
#include <string>

namespace TgBot {
    void to_json(json& j, const UploadStickerFileRequest& value) {
        j = json{};
        j["user_id"] = value.user_id;
        j["sticker_format"] = value.sticker_format;
    }

    void from_json(const json& j, UploadStickerFileRequest& value) {
        if (j.contains("user_id")) {
            j.at("user_id").get_to(value.user_id);
        }
        if (j.contains("sticker_format")) {
            j.at("sticker_format").get_to(value.sticker_format);
        }
    }


    std::string toMultipart(const UploadStickerFileRequest& value, const std::string& boundary) {
        std::string body;

        // Non-file field: user_id
        body += "--" + boundary + "\r\n";
        body += "Content-Disposition: form-data; name=\"user_id\"\r\n\r\n";
        body += std::to_string(value.user_id) + "\r\n";

        // Non-file field: sticker_format
        body += "--" + boundary + "\r\n";
        body += "Content-Disposition: form-data; name=\"sticker_format\"\r\n\r\n";
        body += value.sticker_format + "\r\n";

        // File field: sticker
        if (value.sticker) {
            body += "--" + boundary + "\r\n";
            body += "Content-Disposition: form-data; name=\"sticker\"; filename=\"" + value.sticker->fileName + "\".\r\n";
            body += "Content-Type: " + value.sticker->mimeType + "\r\n\r\n";
            body += value.sticker->data + "\r\n";
        }

        body += "--" + boundary + "--\r\n";
        return body;
    }
}
