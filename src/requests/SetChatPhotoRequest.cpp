// Auto-generated Telegram Bot API models for C++
// Generated by telegram-api-generator. Do not edit manually.

#include "requests/SetChatPhotoRequest.hpp"
#include <string>

namespace TgBot {
    void to_json(json& j, const SetChatPhotoRequest& value) {
        j = json{};
        j["chat_id"] = value.chat_id;
    }

    void from_json(const json& j, SetChatPhotoRequest& value) {
        if (j.contains("chat_id")) {
            j.at("chat_id").get_to(value.chat_id);
        }
    }


    std::string toMultipart(const SetChatPhotoRequest& value, const std::string& boundary) {
        std::string body;

        // Non-file field: chat_id
        body += "--" + boundary + "\r\n";
        body += "Content-Disposition: form-data; name=\"chat_id\"\r\n\r\n";
        body += std::to_string(value.chat_id) + "\r\n";

        // File field: photo
        if (value.photo) {
            body += "--" + boundary + "\r\n";
            body += "Content-Disposition: form-data; name=\"photo\"; filename=\"" + value.photo->fileName + "\".\r\n";
            body += "Content-Type: " + value.photo->mimeType + "\r\n\r\n";
            body += value.photo->data + "\r\n";
        }

        body += "--" + boundary + "--\r\n";
        return body;
    }
}
