// Auto-generated Telegram Bot API models for C++
// Generated by telegram-api-generator. Do not edit manually.

#pragma once
#include <cstdint>
#include <string>
#include <vector>
#include <memory>
#include <nlohmann/json.hpp>
#include "types/BackgroundFill.hpp"

namespace TgBot {

    using json = nlohmann::json;

    /**
     * The background is a freeform gradient that rotates after every message in the chat.
     *
     * @param type Type of the background fill, always “freeform_gradient”
     * @param colors A list of the 3 or 4 base colors that are used to generate the freeform gradient in the RGB24 format
     */
    struct BackgroundFillFreeformGradient : public BackgroundFill {
        typedef std::shared_ptr<BackgroundFillFreeformGradient> Ptr;
        typedef std::weak_ptr<BackgroundFillFreeformGradient> WeakPtr;

        virtual ~BackgroundFillFreeformGradient() = default;
        // Type of the background fill, always “freeform_gradient”
        std::string type_ = "";

        // A list of the 3 or 4 base colors that are used to generate the freeform gradient in the RGB24 format
        std::vector<std::int64_t> colors = std::vector<std::int64_t>();
    };
    void to_json(json& j, const BackgroundFillFreeformGradient& value);
    void from_json(const json& j, BackgroundFillFreeformGradient& value);
}
