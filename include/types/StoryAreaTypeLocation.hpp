// Auto-generated Telegram Bot API models for C++
// Generated by telegram-api-generator. Do not edit manually.

#pragma once
#include <cstdint>
#include <string>
#include <vector>
#include <memory>
#include <nlohmann/json.hpp>
#include "types/StoryAreaType.hpp"
#include "types/LocationAddress.hpp"

namespace TgBot {

    using json = nlohmann::json;

    /**
     * Describes a story area pointing to a location. Currently, a story can have up to 10 location areas.
     *
     * @param type Type of the area, always “location”
     * @param latitude Location latitude in degrees
     * @param longitude Location longitude in degrees
     * @param address Optional. Address of the location
     */
    struct StoryAreaTypeLocation : public StoryAreaType {
        typedef std::shared_ptr<StoryAreaTypeLocation> Ptr;
        typedef std::weak_ptr<StoryAreaTypeLocation> WeakPtr;

        virtual ~StoryAreaTypeLocation() = default;
        // Type of the area, always “location”
        std::string type_;

        // Location latitude in degrees
        double latitude = 0.0;

        // Location longitude in degrees
        double longitude = 0.0;

        // Optional. Address of the location
        LocationAddress::Ptr address;
    };
    void to_json(json& j, const StoryAreaTypeLocation& value);
    void from_json(const json& j, StoryAreaTypeLocation& value);
}
